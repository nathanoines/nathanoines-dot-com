---
deployment:
  tasks:
    - export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && cd /home/nathanoines/public_html && npm install --production=false
    - export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && cd /home/nathanoines/public_html && npm run build
